import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  post: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  voteContainer: {
    flexDirection: 'column',
    alignItems: 'center',
    marginRight: 20,
  },
});
